CookBook:

add(recipe)
   get_recipe(recipe)
   ingredient_adder(ingredients, recipe)
      inventory.add(ingredients, F, recipe)
      inventory.in_stock(ingredients)
   put_item(recipe_item)

remove(recipe)
   get_recipe(recipe)
   remove_from_ing_recipes(recipe, ingredients)
      inventory.remove_recipe(recipe, ingredient)
   delete_item(recipe_item)

get_recipe(recipe)
   query(query_condition)

update_recipe(recipe)
   get_recipe(recipe)
   add(recipe)
   update_ingredients(recipe_item, recipe)
      alter_ingredients(ingredients)
   update_item(recipe_item)

Inventory:

add(ingredient, inStock, sourceRecipe)
   handle_existing_item(item, ingredient, sourceRecipe)
      add_recipe(ingredient_item, sourceRecipe)
      init_recipe(ingredient, sourceRecipe)
      update_stock(ingredient, true)
   put_item(ingredient_item)

find(ingredient)
   query(query_condition)

update_stock(ingredient, stockValue)
   update_recipes(recipes, stockValue)
      cookbook.update_availability(recipe, inStock)
   update_item(ingredient_item)

in_stock(ingredient)
   find(ingredient)

delete(ingredient)
   delete_item(ingredient_item)

remove_recipe(recipe, ingredient)
   find(ingredient)
   update_item(ingredient_item)